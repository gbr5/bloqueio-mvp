// TEMPORARY TESTING: Use this as the main export in page.tsx to test UI flow
// Replace: export default function BloqueioPage() {
// With: export { default } from './router';

export { default } from './router';
